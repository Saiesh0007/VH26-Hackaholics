class AlertService {
  void triggerSpikeAlert() {
    // In production, triggers subtle audio or system vibration
  }

  void triggerPolicyChangeAlert() {
    // In production, triggers subtle haptic tick
  }

  void triggerRecoveryAlert() {
    // In production, triggers success audio cue
  }
}
