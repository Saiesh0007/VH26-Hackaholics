name: pulseflow
description: "AdaptQ - Adaptive AI Data Pipeline Command Center"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  fl_chart: ^0.68.0
  flutter_animate: ^4.5.0
  google_fonts: ^6.1.0
  dio: ^5.4.3+1
  web_socket_channel: ^3.0.0
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
